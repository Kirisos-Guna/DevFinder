export const images = {
  remotePatterns: [
    {
      protocol: "https",
      hostname: "avatars.githubusercontent.com",
      
    }
  ]

};
const nextConfig = {
  output: 'export',
};

export default nextConfig;